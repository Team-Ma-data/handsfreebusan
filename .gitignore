# ─── Secrets / local config (NEVER commit) ───
.env
.env.*
!.env.example
local.properties
*.keystore
*.jks

# ─── Organizer-provided data — do NOT publish ───
# (excluded from this repo; keep it out even if someone copies it back in)
발제사 데이터/
*발제사*

# ─── Python (backend) ───
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ─── Android / Gradle ───
.gradle/
build/
/android/build/
/android/app/build/
.idea/
*.iml
.externalNativeBuild/
.cxx/
captures/

# ─── OS / editor ───
.DS_Store
Thumbs.db
